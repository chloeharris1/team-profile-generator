const inquirer = require("inquirer")
const fs = require("fs")

// Require Employee classes 
const Manager = require("./lib/Manager")
const Engineer = require("./lib/Engineer")
const Intern = require("./lib/Intern")

// Require Generate HTML
const GenerateHtml = require("./lib/generateHTML")

// Create arrays to store user input 
let managerArr = []
let engineerArr = []
let internArr = []


// Inquirer Prompt Start

// Manager Input, push User input to Manager Array 

// Ask User how they want to proceed, provide Choices 

// Engineer Input, push User input to Engineer Array 

// Intern Input, push User input to Intern Array